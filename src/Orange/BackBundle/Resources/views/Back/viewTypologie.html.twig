{% extends "OrangeCoreBundle::layout.html.twig" %}

{% block title %}Admin - DCR{% endblock %}

{% block content %}
    <div class="container">
        <div class="starter-template">
            <h2 style="text-align : center;">Typologies</h2>
            <div class="row">
                <div class="col-md-8 col-md-offset-2">
                    <div class="row">
                        <div class="col-md-3 col-md-offset-9">
                            <a href="{{ path('_typologie_create') }}" class="btn btn-default" style="margin-bottom: 20px;"><i class="icon-plus-sign-alt"></i> Nouvelle typologie</a>
                        </div>
                    </div>
                    <table id="all-table" class="table table-hover table-bordered table-stripped">
                        <thead>
                        <tr>
                            <th>Identifiant</th>
                            <th>Libellé</th>
                            <th>Classification</th>
                            <th>Route</th>
                            <th>Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for t in typo %}
                            <tr>
                                <td>{{ t.id }}</td>
                                <td>{{ t.libelle }}</td>
                                <td>{{ t.classification.libelle }}</td>
                                <td>{{ t.route }}</td>
                                <td><a href="{{ path('_typologie_update', {'id': t.id}) }}"><i class="fa fa-fw fa-pencil"></i></a> <a href="{{ path('_typologie_delete', {'id': t.id}) }}"><i class="fa fa-fw fa-trash"></i></a></td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}