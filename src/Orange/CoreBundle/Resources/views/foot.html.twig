<footer>
    <div class="navbar navbar-inverse">
        <div class="container">
            <div class="row">
                <div class="col-xs-4 col-sm-3">
                            <span class="navbar-brand">&copy;
                                {% set actual = "now"|date('Y') %}
                                {% if actual == 2016 %}
                                    {{ actual }}
                                {% else %}
                                    2016 - {{ actual }}
                                {% endif %}
                                Orange</span>
                </div>
                <div class="col-xs-2 col-xs-offset-6 col-sm-offset-7 col-lg-3 col-lg-offset-8 collapse navbar-collapse">
                    <ul class="nav navbar-nav">
                        <li class="active">
                            <a href="{{ path('_back') }}" class="navbar-brand">Admin</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</footer>